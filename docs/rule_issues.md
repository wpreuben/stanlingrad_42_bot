# 규칙·자료 확인 사항

| 상태 | 범위 | 확인할 내용 |
|---|---|---|
| TODO_RULE_REVIEW | Map A 전체 | `Stal42_Map_west-FINAL-150 Q12.jpg`(3300×5100) 원본을 확보했다. 모든 인쇄 헥스 ID와 각 변의 지형·도로·철도를 구역별로 전사·검수해야 한다. 검수 전에는 `map_a.json`을 완성 자료로 만들지 않는다. |
| TODO_RULE_REVIEW | 아조프해 해안 헥스 | `2038`·`2039`는 번호가 바다 위에 있지만 밝은 육지 쐐기가 경계에 걸친다. §2.2.3은 어두운 해안 조각만 무시하고, 통행 불가 변은 바다로 완전히 덮인 경우에 한정한다. 육상 그래프에 넣을지와 인접 변별 통과 가능성을 원본 확대 그림에서 확정해야 한다. `2338`은 S1 시작 카드의 예이스크 배치 헥스이고 밝은 육지 돌출부가 보인다. |
| RESOLVED | S1.2 5예비군 | 2019년 소련군 시작 카드는 3턴 동쪽 가장자리 `4210` 북쪽 진입이라고 적지만, 영문 규칙 v2.1 S1.2는 3턴 Entry Area M, `4100`~`4111` 사이로 지정한다. 엔진은 2025년 S1.2를 적용한다. 시작 지도 배치에 넣지 않는다. |
| TODO_RULE_REVIEW | S1 일정 자료 | `3806`의 소련군 6예비군 4개는 2턴부터 정상 이동할 수 있다. 5예비군 5개는 3턴 증원이다. 현재 `ScenarioDef`는 초기 배치만 보관하므로 이후 규칙 단위에서 해제·증원 일정을 명시적으로 모델링해야 한다. |
| TODO_RULE_REVIEW | S1 단계 수·면 상태 | `docs/s1_step_face_review.csv`에 두 면이 있는 비사령부 128개의 줄무늬 측정 결과를 임시 기록했다. 반대 면 미확인 32개는 1단계로 추정하지 않는다. 포병 지원 사령부 9개는 §18.2에 따라 1단계 준비/사용 면으로, 보급 포인트는 §18.6.2에 따라 완전 이동/준비 면으로 확인했다. 잔존 병력 연결과 각 면의 능력은 추가 판독이 필요하다. |
| TODO_RULE_REVIEW | S1 병과 기호 | `docs/s1_unit_type_review.csv`에 171개 시작 카운터의 잠정 병과를 정리했다. 대부분은 파일명 추론이므로 기호 전수 검수가 필요하다. 소련군 `1 TD`·`2 TD`는 playbook 24쪽의 Tank Destroyer 기호와 대조해 확인했다. |
| RESOLVED | 소련군 `1 TD`·`2 TD` 병과 | 제공된 `Stal42_Playbook.pdf` 24쪽의 Tank Destroyer 삼각형·점 기호가 두 이름 붙은 카운터 이미지와 일치한다. 2025년 영문 규칙 §2.3.3과 §9.2.4의 대전차 병과로 기록한다. |
| RESOLVED | S1 초기 감소 면 | 171개 시작 카운터의 이름 붙은 이미지와 시작 카드를 대조했다. 소련군 `261`(`2425`)의 시작 면을 바로잡아 24개가 개별 파일의 `-r` 면으로 시작한다. 시작 면, 다른 면, 수치 및 출처는 `docs/s1_counter_named_images.csv`에 기록했다. 이미지가 없다는 이유로 남은 카운터를 단일 단계로 추정하지 않는다. |
| RESOLVED | 추축군 `5`(`1936`) | 추가된 `images_high/RO-5Cav-Div-f.png`의 `3-3-5` 면이 시작 카드와 일치한다. 다른 면은 `RO-5Cav-Div-r.png`다. |
| RESOLVED | S1 개별 카운터 미대조 5개 | 추가된 이름 붙은 파일에서 소련군 `277`·`1 TD`(`1816`), `5`(`2120`), `275`(`2023`), `4`(`2425`)를 찾았다. 시작 카드의 `1 TD`는 `SO-1TD-Div-f.png`이며 비슷한 `2 TD`와 다른 유닛이다. 이제 171개 모두 시작 면이 확인됐다. |
| RESOLVED | S1 카드 표기 판독 2개 | 이름 붙은 이미지와 시작 카드 확대 대조로 추축군 `454`(`1421`) 및 소련군 `335+341`(`3322`)을 확인했다. 초기 CSV의 `45A`, `335+347`을 수정했다. |
