# 규칙·자료 확인 사항

| 상태 | 범위 | 확인할 내용 |
|---|---|---|
| TODO_RULE_REVIEW | Map A 전체 | `Stal42_Map_west-FINAL-150 Q12.jpg`(3300×5100) 원본을 확보했다. 모든 인쇄 헥스 ID와 각 변의 지형·도로·철도를 구역별로 전사·검수해야 한다. 검수 전에는 `map_a.json`을 완성 자료로 만들지 않는다. |
| RESOLVED | S1.2 5예비군 | 2019년 소련군 시작 카드는 3턴 동쪽 가장자리 `4210` 북쪽 진입이라고 적지만, 영문 규칙 v2.1 S1.2는 3턴 Entry Area M, `4100`~`4111` 사이로 지정한다. 엔진은 2025년 S1.2를 적용한다. 시작 지도 배치에 넣지 않는다. |
| TODO_RULE_REVIEW | S1 일정 자료 | `3806`의 소련군 6예비군 4개는 2턴부터 정상 이동할 수 있다. 5예비군 5개는 3턴 증원이다. 현재 `ScenarioDef`는 초기 배치만 보관하므로 이후 규칙 단위에서 해제·증원 일정을 명시적으로 모델링해야 한다. |
| RESOLVED | S1 초기 감소 면 | 소련군 `160`(`1502`)·`62`(`1305`)·`6`(`1604`)·`227`(`1309`)·`301`(`1311`)와 추축군 `23`·`29`(`1515`), `14`(`2029`), `13`·`125`(`2533`), `73`(`2634`)는 개별 카운터의 감소된 면으로 시작한다. 시작 카드의 수치만으로 강한 면이나 최대 단계 수를 대입하지 않는다. 나머지 시작 카운터도 면별로 계속 확인한다. |
| TODO_RULE_REVIEW | 추축군 `5`(`1936`) | 시작 카드에는 `3-3-5` 카운터가 보이지만 제공된 개별 85×85 이미지에서 같은 그림·수치의 카운터를 아직 찾지 못했다. 이미지가 없다는 이유로 임의의 뒷면·단계 수를 만들지 않는다. |
| TODO_RULE_REVIEW | S1 개별 카운터 미대조 5개 | 소련군 `277`·`1 TD`(`1816`), `5`(`2120`), `275`(`2023`), `4`(`2425`)의 시작 카드 그림과 일치하는 개별 85×85 이미지를 아직 확정하지 못했다. 특히 `1 TD`와 비슷한 `2 TD` 개별 이미지는 다른 표기로 유지한다. 양측 카드의 171개 중 165개만 이미지 대조를 마쳤으며, 빠진 6개를 임의의 유닛 정의로 채우지 않는다. |
